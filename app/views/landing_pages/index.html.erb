<% content_for :title, "Influencer Connect" %>

  <%= render 'landing_pages/hero' %>
  <%= render 'landing_pages/creator_list' %>
  <%= render 'landing_pages/why_choose' %>

  <!-- Trusted by Brands Section -->
  <%= render 'landing_pages/brand_section' %>

  <!-- Call to Action Section -->
  <%= render 'landing_pages/demo_book_section' %>
